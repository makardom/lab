#include <iostream>

#include "Dialog.h"

int main() {
    if (Menu()==-1)
        return -1;
    return 0;

//    Map::MyMap<int, int> prob;
//    prob.insert(2, 5);
//    prob.insert(6, 10);
//    for (auto &it: prob){
//        std::cout<<(it).key<<"  "<<it.info<<std::endl;
//    }
}
